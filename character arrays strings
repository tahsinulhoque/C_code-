#include<stdio.h>
int main (){
    char name [20];
    printf("Enter your name :\n");
    gets(name); // gets() reads an entire line including spaces
    printf("Hello ! %s\n", name);
    return 0;
}
